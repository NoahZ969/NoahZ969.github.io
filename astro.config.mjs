import { defineConfig } from 'astro/config';

// Repo will be NoahZ969.github.io, so the site lives at the root of the domain
// and no `base` path is needed.
export default defineConfig({
  site: 'https://noahz969.github.io',
});
